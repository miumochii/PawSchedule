package martinvergara_diegoboggle.pawschedule.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Principales (Brand Colors)
val PawPrimary = Color(0xFF2DD4BF)      // Un turquesa menta suave y moderno
val PawOnPrimary = Color(0xFFFFFFFF)    // Texto sobre el primario
val PawPrimaryContainer = Color(0xFFF0FDFA) // Fondo muy suave del color primario

val PawSecondary = Color(0xFFFB923C)    // Naranja cálido (para acciones como "Eliminar" o alertas)
val PawOnSecondary = Color(0xFFFFFFFF)

val PawTertiary = Color(0xFF818CF8)     // Un lila suave para detalles extra

// Fondos y Superficies
val PawBackground = Color(0xFFF9FAFB)   // No es blanco puro, es un gris muy cálido y suave (descansa la vista)
val PawSurface = Color(0xFFFFFFFF)      // Blanco puro para las tarjetas (Cards)
val PawOnSurface = Color(0xFF1F2937)    // Gris oscuro para texto (nunca uses negro #000000 puro, es muy duro)
val PawOnSurfaceVariant = Color(0xFF6B7280) // Gris medio para subtítulos

val PawOutline = Color(0xFFE5E7EB)      // Bordes muy sutiles